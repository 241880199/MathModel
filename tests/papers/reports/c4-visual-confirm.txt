人工目视确认（Task 4 · C4 闸门）
日期：2026-09-24
目视人：用户（本人）
转录：本文件由 AI 依用户的口述结论落盘——**AI 未、也不能看图**；
      结论的所有权归目视人，措辞力求不增不减。

## 受检样件

`tests/papers/reports/c4-*.png` 共 **6 张**（Task 4 送检样件，`4330eb5` 引入）：
  c4-2500836-fig-18-p19.png   c4-2504218-fig-04-p7.png   c4-2513314-fig-01-p3.png
  c4-2516695-fig-01-p3.png    c4-2517199-fig-09-p14.png  c4-2519935-fig-25-p23.png

这 6 张是全库**偏红像素占比最高**的一批（23.04%–44.44%，见 `c4-proxy-evidence.txt`），
即机器筛选出的最可疑样件。

## 结论

**6 张送检样件均无水印**（无红色「校苑数模公众号」字样）。

**一处必须写明的区分**：同一目录下的 `a4-2500836-with-wm.png` **确实带水印**，
但它是 **Task 2 的对照图**（`e3f6f45` 引入），与本次 C4 闸门**无关**——
它按设计**就要**带水印，与 `a4-2500836-no-wm.png` 同页同参数、只差 `strip_in_memory` 一步，
用「处理前/处理后」的成对呈现来证明置空那一步真的起作用。
它的目视结论早已记在 `wm-visual-confirm.txt`（2026-09-23）。

用户原话：「a4-2500836-with-wm.png 仍有水印，其他图片都没有」
—— 即该目录 **8 张**图里，7 张干净，唯一带水印的那张是**刻意保留的对照**。

## 本闸门的效力边界（不得被读大）

- 本结论只覆盖**这 6 张送检样件**，**不是**对全部 908 张产出图的普查。
  908 张里只有 118 张（偏红 ≥1%）被列为「待人工确认」；本次目视的是其中偏红最高的 6 张。
- 本结论**不**声称「图表抽取的内容框对了」——那需要一个能看图的独立参照，本轮没有（见
  `task-4-report.md` §6 / `c-mutation-evidence.txt` 的「本轮未能证明的东西」）。
- 本文件**不由** `verify_c.py` 生成：该脚本每次运行都会重写 `c-report.txt`，
  结论若只写在报告里，任何一次重跑都会把它抹掉。脚本改为**读**本文件并逐字抄进报告，
  缺了则判本闸门 FAIL——这样这份证据既不会被重跑覆盖，也不会被静默忽略。

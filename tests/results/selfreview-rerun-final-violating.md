# `mcm-selfreview` 复跑证据（终审修复补完后）—— 违规样稿

- 日期：2026-09-22（终审修复补完轮）
- 被审稿件：`tests/fixtures/violating-paper.tex`（**复制**到仓库外临时目录后审查，未移动既有文件）
- 隔离方式：`mktemp -d` → `C:\Users\SHAMEL~1\AppData\Local\Temp\tmp.8aeo4HE3OR\violating-paper.tex`
- 执行者：全新 `general-purpose` subagent（**非** `fork`），只给 `SKILL.md` 与 fixture 两个路径
- 约束：不读 `INDEX.md`、不读 `tests/`、不读 `.superpowers/`、不修改任何文件
- 与上一版复跑的差别：本轮 skill 已加入「前置条件未满足」条款与 A12 的缺失分支条文

**验收结论：6 项植入违规全部命中（A3、A4、A5、A6、A8、A11）；两条诱饵（缺 TOC、CSDN）均未被报成 A 组违规；新增的 A15 未误报（`\Problem=C` 判通过）；A 组 15/15 行无跳过。**

**本轮新条文是否被用到**：用到。A12 的判词直接引用了本轮新增的「触发成立而 `Report on Use of AI` 章节整体缺失 → A12 判不通过」——见其 A12 行「触发成立而 `Report on Use of AI` 章节**整体缺失**」的措辞。

---

## 逐字原文（subagent 最终报告）

# 用 mcm-selfreview skill 复核 `violating-paper.tex` 的结果

## 开工输入状态
六项稿件外证据（A1 页数、A2 摘要单页、A6 实际文件名、A7 体积、A10 附件、A14 题面）本次**均未提供**；目录内只有一份 `.tex` 源码（已 `ls` 确认无 PDF、无编译产物）。故 A1／A2／A7／A10／A14 按 skill 规则判 `未判定`（不估算、不默认通过）；A6 因源码**正向声明非队号文件名**可直接判；AI 使用情况一项稿件正文自带证据。

---

## A 组｜合规（一票否决，逐行必填）

| # | 检查项 | 检查值 | 是否通过 | 证据位置 |
| :--- | :--- | :--- | :--- | :--- |
| A1 | 总页数 ≤25 | 未判定（无编译产物；须用户报网页端编译器的**实际页数**。不得按行数／节数／图表数估算，也不得以页眉 `Page N` 反推——页眉自正文起算，比实际提交页数少约 1） | **未判定** | 无 PDF 可读；须由用户报告（稿内页眉计数 :68 `\setcounter{page}{1}`、:21/:69）｜[官方] §2.1.1；[社区] 操作纪律 |
| A2 | Summary Sheet（第 1 页且摘要容于单页） | 未判定（摘要是否容于单页属**编译后版式属性**，源码不可确定，不得用"约 14 行、单页可容"这类启发式代填） | **未判定** | 无 PDF；须由用户报告（摘要区 :40–60；`:30 \thispagestyle{empty}` 本身非违规）｜[官方] §2.2.1、§2.2.2；[社区] |
| A3 | 摘要页队号栏 | 队号栏**渲染为空**：`\Large \mbox{}` 渲染为空白；全文亦未定义 `\Team` | **不通过** | `violating-paper.tex:35`（`\textbf{Team Control Number}\\ \Large \mbox{}`）｜[官方] §2.2.1、§2.3.1 |
| A4 | 页眉（队号 + 页码，缺一不可） | 页眉**只有页码、无队号**：`\lhead{}` 为空，`\rhead{Page \thepage}`；全文无任何队号。（未写 `of 25` 不构成违规，属示例文字） | **不通过** | `:20`、`:21`、`:69`｜[官方] §2.3.1（另见 §3.1 冲突 4） |
| A5 | 匿名 | 致谢段落出现**机构名**「Example University」，属可识别信息（队号除外） | **不通过** | `:168`（"We thank the faculty of **Example University** for support and for access to the computing cluster"）｜[官方] §2.3.2、§2.3.3 |
| A6 | 提交物与文件名 | 源码**正向声明**落盘名为 `final_paper_v3.pdf`（非队号）；队号本身空缺，更谈不上"名＝队号" | **不通过** | `:1`（`% filename: final_paper_v3.pdf`），旁证 `:3`（"rename this before the portal upload if we have time"）｜[官方] §2.3.4；[社区]（正向即判不通过） |
| A7 | PDF 体积 <25MB | 未判定（无 PDF 可测，"无法判定"亦不得用来打发本条） | **未判定** | 无 PDF；须由用户报告｜[官方] §2.3.7；[社区] |
| A8 | 字号 ≥12pt | 全局 10pt（摘要页与正文同一文档类设置），低于 12pt | **不通过** | `:6`（`\documentclass[10pt]{article}`）｜[官方] §2.3.5、§2.2.3 |
| A9 | 语言（正文与摘要均英文） | 摘要（:44–57）与正文（:72–169）均为英文，判据范围内满足 | **通过** | `:44–57`、`:72–169`｜[官方] §2.3.5、§2.2.4（**边界提示**：`:182` 文献条目标题为中文，判据只写"正文与摘要"，文献条目不在其内，不据此判 A 组不通过——见歧义 2） |
| A10 | 附件范围 | 未判定（"是否随解答另附程序／软件／数据库／其他非论文文件"属**提交动作**证据，稿内不可见；不得因稿内未见痕迹判通过） | **未判定** | 无提交记录；须由用户报告（附录 A 代码为**稿内内联** `:190–199`，按 §2.1.1 计入 25 页，**不属** A10 违规）｜[官方] §2.3.9；[社区] |
| A11 | AI 披露齐备（三处） | 证据显示**用过 AI**：摘要自述"computed with the assistance of **ChatGPT**"；三处披露**全缺**——① 正文无 inline citation（`:72–169` 零标注）② References 无任何 AI 工具条目（`:171–188` 仅 4 条，均非 AI 工具）③ **无** `Report on Use of AI` 章节（文档 `:202` 结束） | **不通过** | `:51`（自述）；`:171–188`；`:190–202`｜[官方] §2.4.2、§2.4.4（边界依据 §2.4.1）；[社区] 操作纪律 |
| A12 | AI 章节位置 | 触发成立而 `Report on Use of AI` 章节**整体缺失**（既不存在，也就无从排在 25 页材料之后） | **不通过** | 全文无该章节（`:190–202` 为附录 A 与 `\end{document}`）｜[官方] §2.4.3、§2.1.5 |
| A13 | 页面尺寸（US Letter 或 A4） | 标准 `article` 类未给 `a4paper`／`letterpaper` 选项，取默认 letterpaper＝US Letter；`geometry`（:7–8）只设边距不改纸张。两种允许值皆可通过，无风险 | **通过** | `:6`（`\documentclass[10pt]{article}`）、`:7–8`｜[官方]† §2.3.6 |
| A14 | 题目特定要求 | 未判定（**题面不在手边**：稿内自述所选为 Problem C（`:17`，渲染于 `:33`），但该题是否自带附加要求——memo／letter、特定解答格式、**更严页数限制**——无法核对；稿内沉默不是履行证据） | **未判定** | 须由队伍对照所选题目自行确认（稿内仅见 `:17 \newcommand{\Problem}{C}`）｜[官方]† §2.1.6；[社区] |
| A15 | 摘要页题号栏 | 题号栏渲染为 **C**（A–F 之一），使用 `\Problem` 宏渲染出正确题号 | **通过** | `:33`（`\textbf{Problem Chosen}\\ \Large \Problem`）＋ `:17`（`\newcommand{\Problem}{C}`）｜[官方] §2.2.9 |

**A 组小结**：通过 3（A9、A13、A15）／不通过 7（A3、A4、A5、A6、A8、A11、A12）／未判定 5（A1、A2、A7、A10、A14）。

**须补的值**：① 网页端编译器显示的**实际页数**（A1，勿用页眉页码推）；② 摘要**是否容于单页**（A2）；③ 提交 PDF **实际体积**（A7）；④ 除这一个 PDF 外**是否另附了非论文文件**（A10）；⑤ **题号与该题附加要求**（A14）。

---

## B 组｜内容质量（不参与"能不能提交"）

| # | 官方内容要求 | 结论 | 位置 |
| :--- | :--- | :--- | :--- |
| B1 | 目录 | **缺**（无 `\tableofcontents`；官方 encouraged、非强制，**不进 A 组**） | 全文 `:1–202` |
| B2 | 问题重述 | **有** | `:72–78` |
| B3 | 变量与假设的清晰陈述 | **部分**（符号表仅 3 个符号；`:124–129` 用到的 $\ell_{ij}$、$q$、$N$、$R$ 未定义） | `:93–103` vs `:124–129` |
| B4 | 假设的合理性与论证 | **有**（三条假设各带论证） | `:82–91` |
| B5 | 问题的分析（论证模型动机） | **部分**（仅 `:74–78` 一段框架；TOPSIS 选型与权重来源无动机论证） | `:74–78`、`:105–129` |
| B6 | 正文只放推导／计算摘要，冗长入附录 | **有**（代码入附录 A，正文无冗长推导） | `:190–199` |
| B7 | 模型检验：误差分析、灵敏度、稳定性 | **缺**（`:76` 把 sensitivity 写进任务，全文无误差分析／灵敏度／稳定性实验；`:152–158` 只是承认确定性弱点） | `:76`、`:152–158`（缺） |
| B8 | 明显优缺点 | **有** | `:152–158` |
| B9 | 结论并明确报告结果 | **有**（目标值 24,318、11.4% 降幅、服务水平 95%） | `:131–150`、`:160–164` |
| B10 | 记录资源与参考文献 | **部分**（文献表 4 条但正文零引用；含 CSDN 博客类来源 `:181–183`——**质量问题、非合规否决项**） | `:171–188` |
| **B11** | **来源记录（`must` 硬义务，自 B10 单列）** | **缺**——正文 `:72–169` 无任何脚注／尾注／正文内标注，文献表 4 条**无一条被正文引用**；而稿件调用了外部来源（`:44–46` 数据集、`:86–88` 车辆厂商标格、`:51` ChatGPT）却无对应标注 → 至少记 `缺` | `:72–169`、`:86–88`、`:171–188` |

**结论行之外的单列提示（两条）**
1. **B11 记「缺」**：来源记录是 `instructions.html:952` 以 `must` 写死的硬义务，正文标注与文献表条目**两处都要**，须补齐（不进 A 组否决，但不应埋在清单里）。
2. **A11「数学软件」边界提示（不作否决依据）**：稿内出现 `Python`（`:52`）与 `numpy`／`scipy`（`:194–195`），属"通用语言与库"这一**边界不清**带，本 skill 不据此判 A 组不通过；建议为避免风险一并披露。

---

```
结论：不能提交（A 组未过：A3、A4、A5、A6、A8、A11、A12；另需补：A1、A2、A7、A10、A14）
```

（用第二形态，并带 `另需补` 分句，因为"不通过"与"未判定"两种状态并存。）

---

# 复核报告（按要求列出的四项）

## 1. A 组表与收尾行
完整 A 组表见上（15 行全覆盖，含指针与分级），收尾行为：
`结论：不能提交（A 组未过：A3、A4、A5、A6、A8、A11、A12；另需补：A1、A2、A7、A10、A14）`

## 2. 判「不通过」的 A 行及证明行号

| 行 | 证明行号（violating-paper.tex） | 证明内容 |
| :--- | :--- | :--- |
| A3 | **:35** | `\textbf{Team Control Number}\\ \Large \mbox{}` —— 队号栏渲染为空 |
| A4 | **:20、:21、:69** | `\lhead{}` / `\rhead{Page \thepage}` —— 页眉只有页码、无队号 |
| A5 | **:168** | "We thank the faculty of **Example University** ..." —— 机构名可识别 |
| A6 | **:1**（旁证 :3） | `% filename: final_paper_v3.pdf` —— 源码正向声明的落盘名不是队号 |
| A8 | **:6** | `\documentclass[10pt]{article}` —— 10pt < 12pt |
| A11 | **:51**（缺项见 :171–188、:190–202） | "computed with the assistance of **ChatGPT**"；正文无 inline citation、References 无 AI 条目、无 `Report on Use of AI` 章节 |
| A12 | **:190–202** | 全文不存在 `Report on Use of AI` 章节（附录 A 之后即 `\end{document}`） |

## 3. 两个点名项
- **(a) 无 Table of Contents** —— **没有**报成 A 组合规失败。官方为 encouraged 非强制，只在 **B1** 提示（记「缺」）。
- **(b) 参考文献含 CSDN 博客链接（`:181–183`）** —— **没有**报成 A 组合规失败。官方未对来源类型设限，只在 **B10** 作质量提示（B10 记「部分」时一并指出）。

## 4. skill 措辞含糊／需自行取舍处
1. **（最关键）A11 触发依据能否来自稿件本身。** skill 三处强调触发依据＝开工输入第 4 项、"不是稿内出现痕迹"，并称该项"无法从稿件本身推断"；同时又规定"稿内痕迹作补充证据，矛盾时从严格一侧"，且 A11 在封闭集内**禁止** `未判定`、缺该项输入时"**不出具任何结论行**"、改出「前置条件未满足」一行。本 fixture 稿件**自己写明用了 ChatGPT（:51）**，既像"痕迹"又像书面自述。我的处置：按"从严格一侧"当作触发成立，判 A11／A12 不通过并出第二形态结论行。**若从严认定触发只能来自开工输入**（本次确实未提供），则按「前置条件未满足」条款，A11／A12 不得裁量、**不得出具任何结论行**，收尾应改为：
   `前置条件未满足：需先确认 AI 工具使用情况（含 LLM／翻译软件／数学软件／代码补全；未用请明确答"无"）`
   两种读法指向同一修复动作，但结论行形态不同。**这是最需要作者钉死的口径。**
2. **A9 的"正文"是否含参考文献。** 判据是"正文与摘要均为英文"，而 `:182` 文献条目标题是中文。文献条目既非正文也非摘要，skill 无对应规则，我判通过并单列提示；若作者意图是"整份 PDF 必须全英文"，则 A9 应判不通过。
3. **A13 证据位置自相矛盾。** skill 把 A13 放进"源码或 PDF 中必有证据、不得写 `未判定`"的封闭集，但页面尺寸实际是编译产物属性；只能从 `:6` 文档类声明推默认纸张。幸好判据本身允许 US Letter 或 A4（两者皆通过），无实际风险；但"源码必有证据"对 A13 不成立。
4. **A6 与 A3 的耦合。** 规则为"声明名＝队号→未判定；≠队号→不通过"。本稿队号根本是空的，"名＝队号"无从成立，我按正向证据判不通过；skill 未描述"队号缺失时 A6 如何判"这一交叉情形。
5. **B11 的"引用了外部来源"门槛。** 规则只说"引用了外部来源而无对应标注 → 至少记 `缺`"。本稿正文零引用但依赖外部事实（厂商标格 `:86–88`、数据集 `:44–46`、ChatGPT `:51`）。把"未标注的外部事实性主张"算作"引用"是我的推断，skill 未给门槛。
6. **`未判定` 行的分级标注。** skill 要求每条标 `[官方]`／`[半官方]`／`[社区]` 与 `†`，判据以 `corpus/official/INDEX.md` §2.1–§2.5 为准；按任务要求只读两个文件，未打开 INDEX.md，故指针与分级全部转引 skill 自带清单。

合规提示（不改变结论）：A 组任一不过即不能提交，修复优先级建议 A3／A4（同一根因：全文无队号）→ A6（改名）→ A11／A12（补三处披露，章节标题须**逐字**为 `Report on Use of AI`，且排在全篇材料之后、不计入 25 页）→ A5（删机构名）→ A8（10pt 改 ≥12pt）。

未修改任何文件；仅读取 skill 定义与 `violating-paper.tex` 两个文件；本机无 PDF、未做编译，也未声称验证过编译或页数。

---

## 逐项判定（本轮的核对结论）

| 验收点 | 结果 |
| :--- | :--- |
| 植入违规 1：摘要页队号栏空（`:35`） | 命中 → A3 不通过 ✓ |
| 植入违规 2：页眉无队号（`:20`） | 命中 → A4 不通过 ✓ |
| 植入违规 3：致谢出现校名（`:168`） | 命中 → A5 不通过 ✓ |
| 植入违规 4：落盘名非队号（`:1`） | 命中 → A6 不通过 ✓ |
| 植入违规 5：字号 10pt（`:6`） | 命中 → A8 不通过 ✓ |
| 植入违规 6：用了 ChatGPT 而无三处披露（`:51`） | 命中 → A11 不通过 ✓（并连带 A12 不通过） |
| 诱饵 1：缺 Table of Contents | **未被报成 A 组违规**（只在 B1 记「缺」）✓ |
| 诱饵 2：CSDN 博客来源 | **未被报成 A 组违规**（只在 B10 作质量提示）✓ |
| 新增 A15 是否误报 | **未误报**：`\Problem=C` 渲染为 `C`，判 **通过** ✓ |
| A 组覆盖面 | **15/15 行逐行给出，无跳过** ✓ |
| 结论行形态 | 第二形态（`不能提交` + `另需补`）✓ |
| 新增 B11 是否生效 | 生效：判 `缺`，并在结论行之外给出显式提示 ✓ |
| 新增 A12 缺失分支条文是否被用到 | 用到：A12 判词即「触发成立而 `Report on Use of AI` 章节整体缺失」✓ |
| 上一轮 I-3 的页码陷阱条文是否生效 | 生效：A1 行写明"不得以页眉 `Page N` 反推——页眉自正文起算，比实际提交页数少约 1"✓ |

**遗留口径问题**：subagent 第 4 节歧义 1 指出——本次**未提供任何队伍输入**，按「前置条件未满足」条款的**严格读法**，A11 不应被裁量、也不应出具结论行；它因稿件自述了 ChatGPT 而选择了"从严格一侧判不通过"。**同一份稿件在提供 3 项输入的场景（见 `selfreview-rerun-final-precondition.md`）里，agent 却正确输出了「前置条件未满足」行。**两种读法的差异已上报，见报告「终审修复补完」的顾虑 1。
